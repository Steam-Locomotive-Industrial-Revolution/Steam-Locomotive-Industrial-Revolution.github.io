{
  "name": "html-tailwind-starter",
  "version": "1.0.0",
  "description": "This is my Yr9 T3 HSIE Assignment. It is a website that details the history, workings, impact, timeline, and legacy of Steam Locomotives, specifically related to the Industrial Revolution.",
  "main": "index.js",
  "watch": {
    "build": {
      "patterns": [
        "index",
        "History",
        "How-it-worked",
        "Impacts",
        "Legacy",
        "Bibliography"
      ],
      "extensions": "html"
    }
  },
  "scripts": {
    "watch": "npm-watch",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "postcss src/styles.css -o dist/styles.css",
    "dev": "concurrently \"npm run build && live-server / --port=8080\" \"npm run watch\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "autoprefixer": "^10.2.4",
    "postcss-cli": "^8.3.1",
    "tailwindcss": "^3.1.4"
  },
  "devDependencies": {
    "concurrently": "^7.6.0",
    "npm-watch": "^0.11.0"
  }
}
